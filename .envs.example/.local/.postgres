# Local development only. See the note in .local/.django.

# PostgreSQL
# ------------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=crm_easyoffice
POSTGRES_USER=localdev
POSTGRES_PASSWORD=localdev
