services:
  docs:
    image: crm_easyoffice_local_docs
    container_name: crm_easyoffice_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - path: ./.envs/.local/.django
        required: false
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./crm_easyoffice:/app/crm_easyoffice:z
    ports:
      - '9000:9000'
    command: /start-docs
